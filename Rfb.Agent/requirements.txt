# Robô de credenciais - Portal Nacional de Tributação sobre Consumo
# Python 3.11+ (tomllib e zoneinfo sao stdlib a partir do 3.11)
#
# client_certificates no launch_persistent_context exige Playwright >= 1.46.
playwright>=1.49.0
python-dotenv>=1.0.1

# Windows: o zoneinfo precisa da base de fusos, que o SO nao fornece.
tzdata>=2024.1; sys_platform == "win32"
